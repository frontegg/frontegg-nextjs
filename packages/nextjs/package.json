{
  "name": "@frontegg/nextjs",
  "libName": "FronteggNextJs",
  "version": "6.7.17",
  "author": "Frontegg LTD",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./tsconfig.build.tsbuildinfo && rm -rf ./node_modules",
    "build": "rm -rf ./tsconfig.build.tsbuildinfo && concurrently -s all \"yarn build:stable\" \"yarn build:types\" && yarn build:copy-files",
    "build:watch": "rm -rf ./tsconfig.build.tsbuildinfo && concurrently -s all \"yarn build:stable --watch\" \"yarn build:types --watch --preserveWatchOutput\"",
    "build:stable": "node ../../scripts/build stable",
    "build:types": "tsc -b tsconfig.build.json",
    "build:copy-files": "node ../../scripts/copy-files.js",
    "test": "cp .env.test .env && CI=true npx playwright test"
  },
  "dependencies": {
    "@frontegg/js": "6.73.0",
    "@frontegg/react-hooks": "6.73.0",
    "pino": "^8.10.0",
    "pino-pretty": "^9.2.0",
    "@types/http-proxy": "^1.17.9",
    "cookie": "^0.5.0",
    "http-proxy": "^1.18.1",
    "iron-session": "^6.3.1",
    "jose": "^4.9.2"
  },
  "devDependencies": {
    "next": ">10.0.0"
  },
  "peerDependencies": {
    "next": ">10.0.0",
    "react": ">16.9.0",
    "react-dom": ">16.9.0"
  }
}
