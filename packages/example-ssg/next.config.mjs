const nextConfig = {
  /* config options here */
  // basePath: '/my-app',
  output: 'export',
  // trailingSlash: true, //process.env.NODE_ENV === 'production',
  reactStrictMode: true,

};

export default nextConfig;